const INITIAL_STATE = {
  name: '',
  assertions: '',
  score: '',
  gravatarEmail: '',
};

export default function user(state = INITIAL_STATE, action) {
  switch (action.type) {
  default:
    return state;
  }
}
